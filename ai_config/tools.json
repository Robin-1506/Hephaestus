[
  {
    "type": "function",
    "function": {
      "name": "get_gas_stations",
      "description": "Récupère les prix des stations. À utiliser dès que l'utilisateur demande des prix.",
      "parameters": {
        "type": "object",
        "properties": {
          "address": { "type": "string", "description": "Ville ou adresse (ex: Strasbourg)" },
          "fuel_type": { "type": "string", "enum": ["Gazole", "SP95", "SP98", "E10"] },
          "radius": { "type": "integer", "default": 10 }
        },
        "required": ["address", "fuel_type"]
      }
    }
  }
]